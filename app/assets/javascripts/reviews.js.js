// Generated by CoffeeScript 1.3.3

jQuery(function() {
  $("#review-form").on("ajax:beforeSend", function(evt, xhr, settings) {
    return $(this).find('textarea').addClass('uneditable-input').attr('disabled', 'disabled');
  }).on("ajax:success", function(evt, data, status, xhr) {
    $(this).find('textarea').removeClass('uneditable-input').removeAttr('disabled', 'disabled').val('');
    return $(xhr.responseText).insertAfter(this).show('slow');
  });
  return $(document).on("ajax:beforeSend", ".review", function() {
    return $(this).fadeTo('fast', 0.5);
  }).on("ajax:success", ".review", function() {
    return $(this).hide('fast');
  }).on("ajax:error", ".review", function() {
    return $(this).fadeTo('fast', 1);
  });
});
